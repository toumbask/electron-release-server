doctype html
html
  head
    title Page not found

    // STYLES
    link(rel="stylesheet", href="/min/production.min.css")
    // STYLES END
  body
    .header
      .navbar.navbar-default(role='navigation')
        .container-fluid
          .navbar-header
            a.pull-left(href='/')
              img.navbar-logo(src='/images/logo.png')
            a.navbar-brand(href='/') The Annoying Tracker
          .collapse.navbar-collapse
            ul.nav.navbar-nav
              li
                a(href='/') Home
            auth-toolbar
    .container
      // Jumbotron
      .jumbotron
        h1
          span.glyphicon.glyphicon-fire.red
          |  404
        p.lead
          | The requested page is not available.
        a.btn.btn-default.btn-lg.text-center(href='/')
          span.green Go Home
